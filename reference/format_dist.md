# Format a distribution object as a string

Format a distribution object as a string

## Usage

``` r
format_dist(x)
```

## Arguments

- x:

  A clm_dist object

## Value

A character string representation
