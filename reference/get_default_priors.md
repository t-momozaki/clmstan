# Get default priors for a threshold structure

Get default priors for a threshold structure

## Usage

``` r
get_default_priors(threshold = "flexible")
```

## Arguments

- threshold:

  A character string specifying the threshold structure ("flexible",
  "equidistant", or "symmetric")

## Value

A list with default prior values
